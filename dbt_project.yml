name: scheduling_case
version: "1.0.0"
config-version: 2
profile: scheduling_case

model-paths: ["models"]
seed-paths: ["seeds"]
test-paths: ["tests"]

models:
  scheduling_case:
    raw:
      +materialized: view
    staging:
      +materialized: view
    intermediate:
      +materialized: view
    marts:
      +materialized: table

seeds:
  scheduling_case:
    +quote_columns: false
